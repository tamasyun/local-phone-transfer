name: Release Windows package

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
    paths:
      - 'src/**'
      - 'scripts/windows/**'
      - 'config/**'
      - 'locales/**'
      - 'packaging/windows/**'
      - '.github/workflows/release-windows.yml'
  workflow_dispatch:
    inputs:
      version:
        description: 'Package version used for a test artifact (for example v0.4.0-test)'
        required: false
        default: 'dev'

permissions:
  contents: write

concurrency:
  group: release-package-${{ github.ref }}
  cancel-in-progress: true

jobs:
  package:
    runs-on: ubuntu-24.04
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version-file: src/go.mod

      - name: Apply Step 3 UI patch
        if: github.ref_type == 'branch' && github.ref_name == 'main'
        shell: bash
        run: |
          set -euo pipefail
          python3 scripts/patch_step3_ui.py
          gofmt -w src/template_admin.go

      - name: Test
        working-directory: src
        run: go test ./...

      - name: Commit Step 3 UI fix
        if: github.ref_type == 'branch' && github.ref_name == 'main'
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git show 8abde67e409be83b8e1de940e35ebdc12179b048:.github/workflows/release-windows.yml > .github/workflows/release-windows.yml
          git rm scripts/patch_step3_ui.py
          git add src/template_admin.go .github/workflows/release-windows.yml
          git commit -m 'Fix wizard return flow and group transfer controls'
          git push origin HEAD:main

      - name: Resolve version
        id: version
        shell: bash
        run: |
          if [[ "$GITHUB_REF_TYPE" == "tag" ]]; then
            VERSION="$GITHUB_REF_NAME"
          elif [[ "$GITHUB_EVENT_NAME" == "workflow_dispatch" && -n '${{ inputs.version }}' ]]; then
            VERSION='${{ inputs.version }}'
          else
            VERSION="dev-${GITHUB_SHA::7}"
          fi
          echo "value=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Build package tree
        shell: bash
        run: |
          set -euo pipefail
          ROOT="dist/OfflineFileTransfer"
          APP="$ROOT/app"
          mkdir -p "$APP/locales"

          # Keep Go symbol/debug metadata. The stripped amd64 binary was flagged
          # by Defender ML, while the non-stripped build passed on the test PC.
          (cd src && GOOS=windows GOARCH=amd64 go build -trimpath -ldflags='-H=windowsgui' -o "../$APP/LocalPhoneTransfer.exe" .)
          (cd src && GOOS=windows GOARCH=arm64 go build -trimpath -ldflags='-H=windowsgui' -o "../$APP/LocalPhoneTransfer_ARM64.exe" .)

          cp packaging/windows/Start.cmd "$ROOT/Start.cmd"
          cp scripts/windows/Bootstrap.ps1 "$APP/Bootstrap.ps1"
          cp scripts/windows/Configure-Firewall.ps1 "$APP/Configure-Firewall.ps1"
          cp scripts/windows/Start-Transfer.ps1 "$APP/Start-Transfer.ps1"
          cp scripts/windows/Update.ps1 "$APP/Update.ps1"
          cp scripts/windows/Launch.cmd "$APP/Launch.cmd"
          cp scripts/windows/Launch-Test.cmd "$APP/Launch-Test.cmd"
          cp config/default.json "$APP/transfer-config.json"
          cp locales/*.json "$APP/locales/"

          printf '%s\n' '${{ steps.version.outputs.value }}' > "$APP/VERSION.txt"
          (cd "$APP" && sha256sum LocalPhoneTransfer.exe LocalPhoneTransfer_ARM64.exe > SHA256SUMS.txt)

          test -f "$ROOT/Start.cmd"
          test -f "$APP/Launch.cmd"
          test -f "$APP/Launch-Test.cmd"
          test -f "$APP/Bootstrap.ps1"
          test -f "$APP/Configure-Firewall.ps1"
          test -f "$APP/Start-Transfer.ps1"
          test -f "$APP/Update.ps1"
          test -f "$APP/transfer-config.json"
          test -f "$APP/locales/ja.json"
          test -f "$APP/locales/en.json"
          test -f "$APP/locales/ja.console.json"
          test -f "$APP/locales/en.console.json"

      - name: Create ZIP
        shell: bash
        run: |
          set -euo pipefail
          VERSION='${{ steps.version.outputs.value }}'
          ZIP="OfflineFileTransfer-${VERSION}.zip"
          (cd dist && zip -r "../$ZIP" OfflineFileTransfer >/dev/null)
          sha256sum "$ZIP" > "$ZIP.sha256"
          echo "ZIP=$ZIP" >> "$GITHUB_ENV"

      - name: Upload package artifact
        uses: actions/upload-artifact@v4
        with:
          name: OfflineFileTransfer-${{ steps.version.outputs.value }}
          if-no-files-found: error
          retention-days: 14
          path: |
            ${{ env.ZIP }}
            ${{ env.ZIP }}.sha256

      - name: Publish GitHub Release
        if: github.ref_type == 'tag'
        env:
          GH_TOKEN: ${{ github.token }}
        shell: bash
        run: |
          set -euo pipefail
          if gh release view "$GITHUB_REF_NAME" >/dev/null 2>&1; then
            gh release upload "$GITHUB_REF_NAME" "$ZIP" "$ZIP.sha256" --clobber
          else
            gh release create "$GITHUB_REF_NAME" "$ZIP" "$ZIP.sha256" --title "Offline File Transfer $GITHUB_REF_NAME" --generate-notes
          fi
